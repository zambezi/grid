export function createSetupGridTemplate() {

  function setupGridTemplate(s) {
    s.html('<h2>grid template</h2>')
  }

  setupGridTemplate.template = function(value) {
    if (!arguments.length) return template
  }

  return setupGridTemplate

}
